apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextjs-app
  labels:
    app: nextjs
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: nextjs
  template:
    metadata:
      labels:
        app: nextjs
    spec:
      containers:
        - name: nextjs
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          env:
            # 🔐 Secret env (sealed secret)
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: seminar-secret
                  key: DATABASE_URL

            # 🌍 Public env (from values.yaml)
            {{- range .Values.env }}
            - name: {{ .name }}
              value: "{{ .value }}"
            {{- end }}

          # 🩺 Readiness Probe (giảm thời gian phản hồi)
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.targetPort }}
            initialDelaySeconds: 3    # Giảm thời gian chờ ban đầu
            periodSeconds: 5          # Kiểm tra sau mỗi 5 giây
            failureThreshold: 2       # Chỉ cho phép thất bại 2 lần
            timeoutSeconds: 2         # Giảm thời gian chờ phản hồi

          # ❤️ Liveness Probe (giảm thời gian phản hồi)
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.targetPort }}
            initialDelaySeconds: 3    # Giảm thời gian chờ ban đầu
            periodSeconds: 5          # Kiểm tra sau mỗi 5 giây
            failureThreshold: 2       # Chỉ cho phép thất bại 2 lần
            timeoutSeconds: 2         # Giảm thời gian chờ phản hồi

      # 🔄 Restart Policy và Backoff Limit
      restartPolicy: Always
      terminationGracePeriodSeconds: 5
